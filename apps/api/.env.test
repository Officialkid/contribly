# Test Environment Configuration
# Separate test database to avoid data loss

# Test database - uses same PostgreSQL instance but different database name
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/contribly_test

# Test environment defaults (set in setup.ts)
NODE_ENV=test
JWT_SECRET=test-jwt-secret-for-integration-tests
FRONTEND_URL=http://localhost:3000
