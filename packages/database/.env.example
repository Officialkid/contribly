# Legacy note:
# Native local development now uses apps/api/.env.local as the canonical
# DATABASE_URL and DIRECT_URL source. Keep this file aligned only if you still use direct
# Prisma tooling from packages/database outside the root helper scripts.
DATABASE_URL=
DIRECT_URL=
